#include <main.h>
/**
 * add - a function that adds two integers and returns the result
 * @one - first numver input
 * @two - second number inp<F11><F11><F11><F11>ut
 * Return: one + two
 */
int add(int one, int two);
{
	return (one + two);
}
